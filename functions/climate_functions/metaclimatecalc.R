metaclimatecalc <- function(metastates, metastart, metaend) {

#climatecalc(getPDSI, metastates, "PDSI", metastart, metaend)
climatecalc(getPR, metastates, "PR", metastart, metaend)
#climatecalc(getTMMN, metastates, metastart, metaend)
#climatecalc(getTMMX, metastates, metastart, metaend)
#climatecalc(getRMIN, metastates, metastart, metaend)
#climatecalc(getRMAX, metastates, metastart, metaend)
#climatecalc(getSRAD, metastates, metastart, metaend)
#climatecalc(getSPH, metastates, metastart, metaend)
#climatecalc(getPET, metastates, metastart, metaend)
#climatecalc(getERC, metastates, metastart, metaend)
#climatecalc(getVS, metastates, metastart, metaend)
#climatecalc(getTH, metastates, metastart, metaend)
#climatecalc(getBI, metastates, metastart, metaend)
#climatecalc(getFM100, metastates, metastart, metaend)
#climatecalc(getFM1000, metastates, metastart, metaend)
#finalfinal <- cbind(PDSIfinal[6], PRfinal[6], PDSIfinal[1:5], PDSIfinal[8:9])
#return(finalfinal)
}

